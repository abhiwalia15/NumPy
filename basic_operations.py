import numpy as np

a=np.array([[1,2,4],[1,4,3]]) #2-d array
b=np.array([2,3,4])

a.dtype #to know the datatype
a.itemsize #byte size
a.ndim #to know dimension
a.size #to know no. of elements
a.shape #(row,columns)
